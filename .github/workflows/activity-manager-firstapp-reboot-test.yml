name: Archived firstAppLaunched Activity Manager reboot test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical test notice
        run: |
          echo "Archived: firstAppLaunched service-callback timing was measured on 2026-09-25."
          echo "Use the recorded results instead of rerunning this disruptive workflow."
