name: Archived direct Activity Manager launch reboot probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical test notice
        run: |
          echo "Archived: direct Activity Manager app-launch timing was measured on 2026-09-25."
          echo "Recorded result: app JS start around 65.5 s boot uptime; production path was earlier."
          echo "Do not rerun this disruptive historical workflow."
